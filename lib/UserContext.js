import { createContext } from 'react';

export const UserContext = createContext(null);
export const UserContextExtra = createContext(null);

export const bDayCardContext = createContext(null);
export const bDayCardNextContext = createContext(null);
export const msgContext = createContext(null);